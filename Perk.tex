% --- Define key family ---
\pgfkeys{
  /perk/.is family, /perk,
  /perk/.cd,
  prereq/.estore in = \perkprereq,
  effect/.estore in = \perkeffect,
}

% --- Define PerkEntry command ---
\newcommand{\PerkEntry}[3]{%
  % #1 = Perk Name
  % #2 = Cost Per Level
  % #3 = Key-value data
  % Load the keys
  \pgfkeys{/pgf/keys/key separator=|, /perk/.cd, #3}%
  % Render the perk name with anchor / index
  \subsection*{\MakePerkAnchor{#1}}%
  \textbf{Pre-Requisites:} \perkprereq\\%
  \textbf{Effect:} \perkeffect\\%
  \textbf{Cost:} #2%
  \medskip
}
